# Data — regenerable / large, never committed
data/

# Results — local only; force-add a specific file to publish it
results/*
!results/.gitkeep
!results/**/.gitkeep
# ...except the benchmark methodology doc, which the README/CODE_AVAILABILITY link to
!results/validation/
results/validation/*
!results/validation/RESULTS_v_benchmark.md

# Local-only working material (kept on disk, not published to the repo)
SPATIAL_KNN_investigation.md
experimental/
validation_notebooks/

# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.eggs/
.ipynb_checkpoints/

# Env / OS
.DS_Store
.venv/
env/
.env

# Metal build artifacts
*.metallib
*.air

# Large reference PDFs kept local
resources/*.pdf

# Claude/internal working docs kept local (not part of the public package)
.claude/skills/
CLAUDE.md
SKILLS.md
RESOURCES.md

# stray build scratch script
scratchpad_build_brain1m_graph.py

# Sphinx docs build output
docs/_build/
docs/api/generated/
